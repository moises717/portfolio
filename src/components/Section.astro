---
interface Props {
	title?: string;
	id?: string;
}

const { title, id } = Astro.props;
---

<div class='mt-9 scroll-m-10' id={id}>
	<>
		<h2 class='text-3xl font-bold text-gray-800 dark:text-white mb-6' aria-label='Titulo de la seccion'>{title}</h2>
		<slot />
	</>
</div>
