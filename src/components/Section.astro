---
interface Props {
	title: string;
	id?: string;
}

const { title, id } = Astro.props;
---

<div class='mt-9' id={id}>
	<>
		<h2 class='text-2xl font-bold text-gray-800 dark:text-white'>{title}</h2>
		<slot />
	</>
</div>
